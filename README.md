# Website Portofolio Pribadi

Halo semuanya! 👋

Perkenalkan, saya **Rifqi Susanto**. Project ini adalah website portofolio pribadi yang saya buat untuk menampilkan profil, pengalaman, daftar project, sertifikat, komentar pengunjung, dan form kontak.

Website ini dibangun menggunakan **Next.js**, **React**, **Tailwind CSS**, **Supabase**, dan **Nodemailer**.

**Live Demo:** [https://k1bot.vercel.app](#)

---

## 🛠️ Tech Stack

Project ini dibuat menggunakan teknologi berikut:

- **Next.js** - Framework React untuk frontend dan routing
- **ReactJS** - Library untuk membangun UI
- **Tailwind CSS** - Utility-first CSS framework
- **Supabase** - Backend untuk data project, sertifikat, komentar, dan storage
- **Nodemailer** - Mengirim pesan dari form kontak ke Gmail
- **Lucide React** - Icon library
- **ESLint** - Menjaga kualitas kode

---

## User Roles

| Role                 | Access                                                                                      |
| -------------------- | ------------------------------------------------------------------------------------------- |
| **Visitor (Public)** | Melihat project, sertifikat, komentar, dan mengirim komentar                                |
| **Admin**            | Mengelola data project, sertifikat, komentar, dan pesan kontak melalui Supabase/Admin panel |

---

## Getting Started

### Prerequisites

Pastikan sudah menginstall:

- Node.js `>= 18.x`
- npm
- Akun Supabase
- Akun Gmail dengan App Password untuk fitur kontak

---

### 1. Clone & Install

```bash
git clone https://github.com/ki1bot/kibot-portofolio.git
cd kibot-portofolio
npm install -D tailwindcss @tailwindcss/postcss postcss
rm -rf node_modules package-lock.json
npm install
```

---

### 2. Environment Variables

Buat file `.env.local` di root project:

```env
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key

GMAIL_USER=your-gmail-address
GMAIL_APP_PASSWORD=your-gmail-app-password
CONTACT_RECEIVER_EMAIL=your-receiver-email
```

---

### 3. Supabase Client

File Supabase client berada di:

```txt
src/lib/supabase/client.js
```

Contoh konfigurasi:

```javascript
import { createClient } from "@supabase/supabase-js";

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
const supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;

export const supabase = createClient(supabaseUrl, supabaseKey);
```

---

### 4. Database Setup

Buka Supabase Dashboard → **SQL Editor** → jalankan query berikut.

```sql
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

CREATE TABLE IF NOT EXISTS public.projects (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title text NOT NULL,
  description text,
  img text,
  link text,
  github text,
  features jsonb DEFAULT '[]'::jsonb,
  tech_stack jsonb DEFAULT '[]'::jsonb,
  is_published boolean DEFAULT true,
  order_index int DEFAULT 0,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.certificates (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title text NOT NULL DEFAULT 'Certificate',
  img text NOT NULL,
  pdf_url text,
  type text NOT NULL DEFAULT 'pdf',
  order_index int DEFAULT 0,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.portfolio_comments (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  content text NOT NULL,
  user_name text NOT NULL,
  profile_image text,
  is_pinned boolean DEFAULT false,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.contact_messages (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  name text NOT NULL,
  email text NOT NULL,
  message text NOT NULL,
  is_read boolean DEFAULT false,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.profiles (
  id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  username text UNIQUE NOT NULL,
  role text NOT NULL CHECK (role IN ('admin', 'user')),
  created_at timestamptz DEFAULT now()
);

CREATE UNIQUE INDEX IF NOT EXISTS projects_title_unique
ON public.projects (title);

CREATE UNIQUE INDEX IF NOT EXISTS certificates_img_unique
ON public.certificates (img);

CREATE UNIQUE INDEX IF NOT EXISTS pinned_admin_comment_unique
ON public.portfolio_comments (user_name, is_pinned)
WHERE is_pinned = true;
```

---

### 5. Row Level Security

Jalankan query berikut untuk mengaktifkan RLS dan policy public.

```sql
ALTER TABLE public.projects ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.certificates ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.portfolio_comments ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.contact_messages ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;

CREATE OR REPLACE FUNCTION public.is_admin()
RETURNS boolean
LANGUAGE sql
SECURITY DEFINER
SET search_path = public
AS $$
  SELECT EXISTS (
    SELECT 1
    FROM public.profiles
    WHERE id = auth.uid()
      AND role = 'admin'
  );
$$;

DROP POLICY IF EXISTS "public read projects" ON public.projects;
DROP POLICY IF EXISTS "admin manage projects" ON public.projects;

DROP POLICY IF EXISTS "public read certificates" ON public.certificates;
DROP POLICY IF EXISTS "admin manage certificates" ON public.certificates;

DROP POLICY IF EXISTS "public read comments" ON public.portfolio_comments;
DROP POLICY IF EXISTS "public insert comment" ON public.portfolio_comments;
DROP POLICY IF EXISTS "admin update comments" ON public.portfolio_comments;
DROP POLICY IF EXISTS "admin delete comments" ON public.portfolio_comments;

DROP POLICY IF EXISTS "public insert contact messages" ON public.contact_messages;
DROP POLICY IF EXISTS "admin read contact messages" ON public.contact_messages;
DROP POLICY IF EXISTS "admin update contact messages" ON public.contact_messages;
DROP POLICY IF EXISTS "admin delete contact messages" ON public.contact_messages;

CREATE POLICY "public read projects"
ON public.projects
FOR SELECT
USING (is_published = true);

CREATE POLICY "admin manage projects"
ON public.projects
FOR ALL
USING (public.is_admin())
WITH CHECK (public.is_admin());

CREATE POLICY "public read certificates"
ON public.certificates
FOR SELECT
USING (true);

CREATE POLICY "admin manage certificates"
ON public.certificates
FOR ALL
USING (public.is_admin())
WITH CHECK (public.is_admin());

CREATE POLICY "public read comments"
ON public.portfolio_comments
FOR SELECT
USING (true);

CREATE POLICY "public insert comment"
ON public.portfolio_comments
FOR INSERT
TO anon, authenticated
WITH CHECK (is_pinned = false);

CREATE POLICY "admin update comments"
ON public.portfolio_comments
FOR UPDATE
USING (public.is_admin())
WITH CHECK (public.is_admin());

CREATE POLICY "admin delete comments"
ON public.portfolio_comments
FOR DELETE
USING (public.is_admin());

CREATE POLICY "public insert contact messages"
ON public.contact_messages
FOR INSERT
TO anon, authenticated
WITH CHECK (true);

CREATE POLICY "admin read contact messages"
ON public.contact_messages
FOR SELECT
USING (public.is_admin());

CREATE POLICY "admin update contact messages"
ON public.contact_messages
FOR UPDATE
USING (public.is_admin())
WITH CHECK (public.is_admin());

CREATE POLICY "admin delete contact messages"
ON public.contact_messages
FOR DELETE
USING (public.is_admin());
```

---

### 6. Storage Setup

Jalankan query berikut untuk membuat bucket storage.

```sql
INSERT INTO storage.buckets (
  id,
  name,
  public,
  file_size_limit,
  allowed_mime_types
)
VALUES
(
  'project-images',
  'project-images',
  true,
  5242880,
  ARRAY['image/jpeg', 'image/png', 'image/webp', 'image/svg+xml']
),
(
  'certificate-images',
  'certificate-images',
  true,
  10485760,
  ARRAY['image/jpeg', 'image/png', 'image/webp', 'application/pdf']
),
(
  'comment-avatars',
  'comment-avatars',
  true,
  2097152,
  ARRAY['image/jpeg', 'image/png', 'image/webp']
)
ON CONFLICT (id) DO UPDATE
SET
  public = EXCLUDED.public,
  file_size_limit = EXCLUDED.file_size_limit,
  allowed_mime_types = EXCLUDED.allowed_mime_types;
```

Jalankan policy storage berikut:

```sql
DROP POLICY IF EXISTS "public read project images" ON storage.objects;
DROP POLICY IF EXISTS "public read certificate images" ON storage.objects;
DROP POLICY IF EXISTS "public read comment avatars" ON storage.objects;
DROP POLICY IF EXISTS "public upload comment avatars" ON storage.objects;

CREATE POLICY "public read project images"
ON storage.objects
FOR SELECT
USING (bucket_id = 'project-images');

CREATE POLICY "public read certificate images"
ON storage.objects
FOR SELECT
USING (bucket_id = 'certificate-images');

CREATE POLICY "public read comment avatars"
ON storage.objects
FOR SELECT
USING (bucket_id = 'comment-avatars');

CREATE POLICY "public upload comment avatars"
ON storage.objects
FOR INSERT
TO anon, authenticated
WITH CHECK (bucket_id = 'comment-avatars');
```

---

### 7. Seed Komentar Admin

```sql
INSERT INTO public.portfolio_comments (
  content,
  user_name,
  profile_image,
  is_pinned
)
VALUES (
  'Halo, terima kasih sudah mampir.',
  'Rifqi',
  '/assets/default-avatar.jpg',
  true
)
ON CONFLICT (user_name, is_pinned)
WHERE is_pinned = true
DO UPDATE SET
  content = EXCLUDED.content,
  profile_image = EXCLUDED.profile_image;
```

---

### 8. Run Locally

```bash
npm run dev
```

Buka di browser:

```txt
http://localhost:3000
```

---

## Pages & Features

### Public Visitor

- **Home** — Hero section, profil singkat, pengalaman, dan about
- **Projects** — Menampilkan daftar project
- **Project Detail** — Menampilkan detail project, tech stack, fitur utama, live demo, dan GitHub
- **Certificates** — Menampilkan daftar sertifikat
- **Comments** — Menampilkan komentar pengunjung dan komentar pinned
- **Contact** — Mengirim pesan melalui form kontak ke Gmail

### Admin

- **Projects** — Mengelola data project melalui Supabase
- **Certificates** — Mengelola data sertifikat melalui Supabase
- **Comments** — Menghapus atau pin komentar melalui Supabase
- **Contact Messages** — Melihat pesan kontak melalui Supabase

---

## Troubleshooting

- Pastikan Node.js sudah terinstall.
- Pastikan sudah menjalankan `npm install`.
- Pastikan file `.env.local` sudah dibuat.
- Restart development server setelah mengubah `.env.local`.
- Jika data tidak muncul, cek URL dan key Supabase.
- Jika komentar gagal dikirim, cek RLS pada tabel `portfolio_comments`.
- Jika form kontak gagal mengirim email, cek `GMAIL_USER`, `GMAIL_APP_PASSWORD`, dan `CONTACT_RECEIVER_EMAIL`.
- Jika gambar tidak tampil saat deploy, pastikan file gambar tersedia di GitHub atau storage publik.

---

## Credits & Contact

**Rifqi Susanto**

GitHub: [ki1bot](https://github.com/ki1bot)

⭐ Jika project ini membantu atau menarik, jangan lupa beri star di GitHub!
